faiss-cpu==1.9.0         # or the latest version that is compatible with your setup
pandas==2.2.3            # or the latest version compatible with your environment
streamlit==1.39.0        # or the latest version compatible with your environment
langchain        # or the latest version compatible with your environment
google-generativeai==0.6.10  # or the latest version compatible with your environment
pydantic==2.9.2          # or the latest version compatible with your environment
pymongo==4.10.1          # or the latest version compatible with your environment
uuid                      # No installation needed, it's part of the standard library
json                      # No installation needed, it's part of the standard library
